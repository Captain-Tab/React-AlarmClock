import * as React from 'react';

class TotalCount extends React.Component<any, any> {
  public render() {
    return (
      <div className="TotalCount" id="TotalCount">
        TotalCount
      </div>
    );
  }
}

export default TotalCount;
